# GEMINI_API_KEY: Required for Gemini AI API calls.
# AI Studio automatically injects this at runtime from user secrets.
# Users configure this via the Secrets panel in the AI Studio UI.
GEMINI_API_KEY="MY_GEMINI_API_KEY"

# PORT: Optional local dev/preview port. Defaults to 3006.
PORT=3006

# PostgreSQL connection string. Use a PostgreSQL database with pgvector enabled.
DATABASE_URL="postgresql://user:password@host:5432/ai_iot_dashboard"
# Set to true for managed Postgres providers that require TLS.
DATABASE_SSL=false

# APP_URL: The URL where this applet is hosted.
# AI Studio automatically injects this at runtime with the Cloud Run service URL.
# Used for self-referential links, OAuth callbacks, and API endpoints.
APP_URL="MY_APP_URL"

# Optional gateway ingest token. If set, gateways must send this token via x-iot-token
# or Authorization: Bearer <token> when POSTing to /api/telemetry.
IOT_INGEST_TOKEN=""

# Optional backend MQTT subscriber defaults. Runtime changes are saved to PostgreSQL
# when DATABASE_URL is configured, otherwise to runtime-config.json for local demos.
MQTT_ENABLED=false
MQTT_BROKER_URL="mqtt://broker.example.com:1883"
MQTT_USERNAME=""
MQTT_PASSWORD=""
MQTT_TOPICS="devices/+/telemetry"
